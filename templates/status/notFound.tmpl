{{template "base/header" .}}
<div class="container slogan text-center">
	<div class="page-header">
		<h1>{{.ErrorTitle}}
			<small>{{.ErrorSmall}}</small></h1>
	</div>
	<p class="lead">{{.ErrorDescription}}</p>
</div>
{{template "base/footer" .}}